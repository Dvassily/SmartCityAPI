# Web API for the project 'Smart City'
Before launch the web API, create the mongoDB database by typing the following command in the utilitary 'mongo' :

> use smartcity

Then, in a terminal, type the following command :
cd database/
sh createdb.sh